# PR 59 live test checklist

Status: prepared, not run. This checklist is evidence preparation, not a pass and not merge authorization.

## Candidate and scope

- PR: https://github.com/achibukz/AIS-OS/pull/59
- Issue: https://github.com/achibukz/AIS-OS/issues/13
- Head: `a093472024e3ab27b2ea9359bbec5300e6d6aa43`
- Branch: `ticket/13-cohesion-onto-main`
- Feature: versioned task and Calendar operations with stable identities, durable receipts, partial retry and completion history.
- Repair under this head: an accepted newer same-item source supersedes older pending operations before the new item state is written. A redelivery of the older source cannot overwrite the newer Calendar event.

## Isolated environment

Use a scratch task register, a temporary cohesion database and one disposable writable Google Calendar. Do not use the real [tasks.md](http://100.106.210.38:8999/Code/GitHub/AIS-OS/tasks.md), the live cohesion database or any calendar containing personal or school appointments.

Record these facts before the first provider write:

- `git rev-parse HEAD` output: ______________________________
- `gws` profile authorized for the disposable Calendar: ______________________________
- Disposable Calendar ID: ______________________________
- Scratch task path: ______________________________
- Temporary SQLite path: ______________________________
- Startup, process owner and binding check: ______________________________
- Calendar event IDs present before the test: ______________________________

Required prerequisites:

- The operator has an existing `gws` OAuth profile with read and write access to the disposable Calendar. Do not paste credentials into chat.
- The operator can run `scripts/cohesion.py` from this branch and can inspect the printed JSON, scratch task file and disposable Calendar.
- No model call is required. The product model is not part of this deterministic CLI test.

## Cases

### 1. Create one linked school deadline

Submit a synthetic `school_deadline` with `placement: both`, a fixed future all-day `due` date, `area: school`, and the disposable Calendar profile and ID recorded above.

Expected:

- The receipt has one applied `tasks` operation and one applied `calendar` operation.
- Both results refer to one `item_id`; the task line has one stable `task_id` marker and the Calendar event has the same private `achios_item_id`.
- The task date and Calendar all-day end date use the expected next-day boundary.

Actual command and output:

```text

```

### 2. Redeliver after a controlled Calendar timeout

Use an operator-approved disposable harness that accepts one Calendar insert and then returns a timeout. If no controlled fault-injection harness exists, mark this case not run. Do not simulate success by changing the receipt.

Resubmit the exact same source after the timeout and inspect the disposable Calendar by the deterministic event ID before any retry.

Expected:

- The first receipt has the task applied and Calendar pending.
- The redelivery reconciles the accepted insert without creating a duplicate event.
- The final receipt has at most one Calendar event for the item.

Actual command and output:

```text

```

### 3. Reschedule the linked item

Submit a new source for the returned `item_id`, keeping the same placement and Calendar target while changing the due date. Inspect both destinations.

Expected:

- The task keeps its original `task_id` marker.
- The Calendar event keeps its original event ID and private item identity.
- The task date and Calendar date change to the new date.

Actual command and output:

```text

```

### 4. Complete by item identity

Submit `action: complete` with the returned `item_id`. Do not complete by title.

Expected:

- The task moves to `## Done` with one completion record.
- The Calendar event remains and records `achiOS item state: completed` without changing its established time or event identity.

Actual command and output:

```text

```

### 5. Confirm the repaired stale-redelivery path

The provider-independent regression already covers this case at this head. It submits an older source whose Calendar write fails, accepts a newer same-item update, then redelivers the older source.

Expected automated result:

- The old Calendar operation is reported as superseded and is not retried.
- The newer Calendar title remains unchanged.
- The fixture records zero Calendar updates during the stale redelivery.

Evidence command and output:

```text
/home/achibukz/.local/share/achios/venv/bin/python -m pytest tests/test_cohesion.py -q
```

Observed at head `a093472024e3ab27b2ea9359bbec5300e6d6aa43`: 21 passed.

## Stop, recovery and cleanup

Stop before writing if the `gws` binary is missing, the profile is not authorized, the Calendar ID is not the recorded disposable ID, or any receipt names an unexpected destination. Stop after a timeout until the event is inspected by deterministic ID. Never repeat a possibly accepted insert blindly.

For a failed case, retain the printed receipt, task file, Calendar event body, command exit status and the exact head. Repair or re-authenticate only within the disposable test scope, then rerun the failed case from its recorded state. A changed head invalidates this checklist's automated and live evidence.

Delete only the temporary database, scratch task file and disposable Calendar events created by this run after recording their final state. Keep this checklist and the redacted interaction record.

## Evidence status

| Gate | Status | Evidence |
|---|---|---|
| Cohesion fixture suite, including stale redelivery | Passed | 21 tests passed at head `a093472024e3ab27b2ea9359bbec5300e6d6aa43` |
| Real `gws` create and linked destinations | Not run | Requires an authorized disposable profile and Calendar |
| Real timeout recovery | Not run | Requires a controlled timeout harness or approved fault injection |
| Real reschedule | Not run | Requires the disposable provider environment |
| Real completion | Not run | Requires the disposable provider environment |
| GitHub checks | Not reported | `gh pr checks 59` reports no checks |
