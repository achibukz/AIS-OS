Assisted live testing of `<head SHA>` for `<feature>`.

Result: `<passed within stated scope / failed / blocked / partial>`.

| Criterion | Result | Evidence |
|---|---|---|
| `<criterion>` | `<status>` | `<receipt, human step or log reference>` |

Automated checks: `<exact command, counts and exit status>`.
Live steps: `<human actions and observed results, distinct from simulation>`.
Environment and actual product model: `<verified values>`.
Failures and unverified gates: `<specific list, or none within the stated scope>`.

Interaction and evidence record: `[report](<accessible URL>)`. `<State any access restriction.>`

This result applies to the named head and cases. `<Identify a newer PR head if present.>`
